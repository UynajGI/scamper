[project]
name = "scamper"
version = "0.1.0"
description = "Development tooling for the Scamper Monte Carlo workspace"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mempalace>=3.0.0",
]
